---
interface Props {
  content: string;
}
const { content } = Astro.props;
const wordCount = content.trim().split(/\s+/).length;
const minutes = Math.max(1, Math.ceil(wordCount / 200));
---
<span class="text-concrete-400 text-sm">{minutes} min read</span>
